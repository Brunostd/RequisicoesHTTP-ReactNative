// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface FilmesStates {
  filmes: FilmeProps[];
}

interface FilmeProps {
  id: number;
  nome: string;
  sinopse: string;
  foto: string;
}
